#include "main.h"
#include <stdio.h>
/**
 * add - Adds two integers and returns the result
 * @x: number been added
 * @y: number been added
 * Return: Always 0.
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
