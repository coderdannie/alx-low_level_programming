#include "main.h"
#include <stdio.h>
/**
 * mul - a function that multiplies two integers
 * @a: input being tested
 * @b: input being tested
 * Return: Always 0
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
